# Nama workflow yang akan muncul di tab Actions
name: 'CI & Discord Notification'

# Kapan workflow ini dijalankan?
on:
  push:
  pull_request:
    branches: [ 'main' ]

# Pekerjaan (job) yang akan dijalankan
jobs:
  # Nama job: build-and-test
  build-and-test:
    runs-on: ubuntu-22.04
    timeout-minutes: 20

    # Langkah-langkah (steps) yang akan dijalankan
    steps:
      # Step 1: Download kode sumber dari repository
      - name: Checkout source code
        uses: actions/checkout@v4

      # Install LLVM via prebuilt binaries (avoids apt-get hangs)
      - name: Install LLVM
        uses: KyleMayes/install-llvm-action@v2
        with:
          version: "14"

      # Ensure llvm-config is available for llvm-sys
      - name: Verify LLVM
        run: |
          echo "LLVM_DIR=$LLVM_DIR"
          "$LLVM_DIR/bin/llvm-config" --version
          echo "LLVM_SYS_140_PREFIX=$LLVM_DIR" >> $GITHUB_ENV

      # Step 2: Install Rust toolchain (compiler, cargo, dll)
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          # Tambahkan komponen clippy untuk analisis kode statis
          components: clippy

      # Step 3: Periksa kode (cepat) untuk cari error sintaks
      - name: Run cargo check
        run: cargo check --verbose

      # Step 4: Jalankan semua test yang ada
      # LLVM JIT tidak thread-safe — multiple Context::create() + JIT
      # secara paralel bisa SIGSEGV. Run single-threaded.
      # RUST_BACKTRACE=full to capture LLVM-level segfaults.
      - name: Run tests
        env:
          RUST_BACKTRACE: "full"
        run: cargo test --verbose -- --test-threads=1

      # Step 4b: Print compiler diag log if it exists (survives SIGSEGV)
      - name: Print diag log
        if: always()
        run: cat /tmp/aha_diag.log 2>/dev/null || true

      # Step 5: Build aplikasi dalam mode release
      - name: Build project
        run: cargo build --release --verbose

      # Step 6: Kirim notifikasi ke Discord (opsional, butuh DISCORD_WEBHOOK_URL secret)
      - name: Notify Discord on success or failure
        if: always() && env.DISCORD_WEBHOOK_URL != ''
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        uses: sarisia/actions-status-discord@v1
        with:
          # Ambil URL webhook dari secret yang kita buat tadi
          webhook: ${{ secrets.DISCORD_WEBHOOK_URL }}
          # Judul notifikasi
          title: "Build Result: ${{ job.status }}"
          # Deskripsi notifikasi, bisa berisi link ke commit
          description: |
            Commit: `${{ github.sha }}`
            Author: ${{ github.actor }}
            Message: ${{ github.event.head_commit.message }}
            [View Workflow Run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})
          # Warna notifikasi (hijau untuk sukses, merah untuk gagal)
          color: ${{ job.status == 'success' && '0x28a745' || '0xdc3545' }}
          # Avatar yang digunakan (logo GitHub)
          avatar_url: https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png
